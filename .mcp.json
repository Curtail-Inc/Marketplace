{
  "mcpServers": {
    "regrade": {
      "type": "http",
      "url": "${REGRADE_API_URL:-https://api.regrade.curtail.com}/api/v1/mcp",
      "headers": {
        "Authorization": "Bearer ${REGRADE_API_KEY}"
      }
    }
  }
}
